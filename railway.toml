# تاسي فيجن — Railway
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway.api"

[deploy]
# No HTTP healthcheck — a failing check was leaving the public URL dead.
# Railway still tracks process uptime via the listening PORT.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
